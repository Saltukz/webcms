@model AlertMessage

<div class="row">
    <div class="col-md-12">
        <div class="alert alert-@Model.AlertType">
            <h4 class="alert-title">@Model.Title</h4>
            <p style="text-align:center;font-weight:900;">@Model.Message</p>
        </div>
    </div>
</div>

